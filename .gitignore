# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
Logs/*.log
*.log

# Outputs (generierte Bilder)
outputs/*.png
outputs/*.jpg
outputs/*.jpeg
outputs/*.webp
!outputs/.gitkeep

# Sensitive Config Files
static/config/bot_token.txt
static/config/bot_id.txt
static/config/bot_owner_id.txt

# Data Files
static/data/prompts_history.txt
static/data/sent_images.txt

# Temporary Files
*.tmp
*.bak
*.swp
.cache/
temp/

# OS Files
Thumbs.db
.DS_Store
desktop.ini
*.lnk

# Model Files
*.ckpt
*.safetensors
*.pth
*.pt
models/
checkpoints/

# Node modules
node_modules/
package-lock.json
yarn.lock

# Environment files
.env
.env.local
```

---

## `.gitkeep` Dateien

### `outputs/.gitkeep`
```
# This file ensures the outputs directory is tracked by Git
# All generated images will be ignored but the folder structure remains
```

### `static/.gitkeep`
```
# This file ensures the static directory is tracked by Git
```

### `Logs/.gitignore`
```
# This file ensures the Logs directory is tracked by Git
# Log files are ignored but the folder structure remains
```

### `docs/screenshots/.gitkeep`
```
# This file ensures the screenshots directory is tracked by Git
```

### `examples/.gitkeep`
```
# This file ensures the examples directory is tracked by Git
```

### `static/assets/.gitkeep`
```
# This file ensures the assets directory is tracked by Git
```

### `static/config/.gitkeep`
```
# This file ensures the config directory is tracked by Git
# Sensitive config files are ignored but the folder structure remains
```

### `static/data/.gitkeep`
```
# This file ensures the data directory is tracked by Git
# Data files are ignored but the folder structure remains
```

### `static/css/.gitkeep`
```
# This file ensures the css directory is tracked by Git
```

### `static/js/.gitkeep`
```
# This file ensures the js directory is tracked by Git
```

### `src/backend/__pycache__/.gitkeep`
```
# This file ensures the __pycache__ directory structure is tracked
# Note: __pycache__ files themselves are ignored